rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Only signed-in users can read or write any data
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
